/**
 * Utility for handling base path
 */

const BASE_PATH = ''

export const getBasePath = () => BASE_PATH

export const getPath = (path: string) => path

export const getAssetPath = (asset: string) => `/assets/${asset}`
